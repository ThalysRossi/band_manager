# Merch Booth

This feature lists sellable inventory variants and supports cash checkout on a dedicated cart route. The cart is scoped by user and band, persists in session storage across refreshes, and reconciles itself against current inventory. Pix and card payment flows are not yet exposed in the web UI.
