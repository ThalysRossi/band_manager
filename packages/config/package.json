{
  "name": "config",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "scripts": {
    "lint": "echo \"No config lint configured yet\"",
    "test": "echo \"No config tests configured yet\""
  }
}
